{
  "name": "node-game-ecs",
  "description": "Lightweight ECS library for node-game-server game logic",
  "author": {
    "email": "guillaume.mathe@gmail.com",
    "name": "Guillaume MATHE"
  },
  "version": "0.0.1",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/types/index.d.ts",
      "import": "./dist/node-game-ecs.js",
      "default": "./dist/node-game-ecs.js"
    },
    "./src/*": "./src/*"
  },
  "module": "./dist/node-game-ecs.js",
  "types": "./dist/types/index.d.ts",
  "sideEffects": false,
  "files": [
    "dist/",
    "src/"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "prebuild": "rm -rf dist",
    "build:bundle": "node build.js",
    "build:types": "tsc",
    "build": "node build.js && tsc",
    "prepublishOnly": "npm run build",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
